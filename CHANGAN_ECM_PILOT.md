# 长安 96S1P 内阻异常样本：首轮 ECM 试验

## 目的与边界

参考 Luder 等《Big data generation platform for battery faults under real-world variances》的故障建模思想：将**同一节电芯**的欧姆电阻 R0、两组极化电阻 R1/R2 同时乘以 α，α 在 1.5–3 之间。论文采用 80S LMO 电池的电、热、老化耦合仿真，并随机化车辆、温度、SOC、制造散差、工况和故障。本试验面向长安 **96S1P NCM** 数据，不能直接套用论文的电芯参数、几何或温度探针位置。

实现采用“实测基线电压 + 双 RC 故障增量”：实测电流、SOC、96 节单体电压、32 个温度探针都保留；随机挑一节电芯和故障强度，只修改这一节电芯的电压。原数据没有逐电芯故障标签，因此**基线健康只是待验证假设**。故障前后电容电压连续，故障后的电阻升高而电容不变。额外电阻发热仅作为模型预测单独保存，**没有合成温度**。这是简化的反事实故障注入，不是论文完整平台的复现，也不能被称为实测故障。

## 数据与方法

- 来源：`H:/mamba/changanDataset/vin102.csv` 前 150000 行。前 100000 行用于标定，后 50000 行用于同车留出验证与候选生成。原始 CSV 无任何改动。
- 采样约 10 秒。用相邻时刻单体电压中位数的变化拟合电流阶跃、40 秒与 300 秒两个极化响应，并用 SOC 差分抵消缓慢 OCV 漂移。两个时间常数是**固定建模假设**，并非 EIS 实测。
- 从留出部分选 8 个互不重叠的连续 256 点片段；每条约 42.7 分钟，故障在第 64 点开始。8 条序列仍只来自**一辆车**，不是 8 个独立车辆样本。
- 候选以 NPZ 单独存放在 `synthetic/changan_ecm/<时间戳>/`，连同 JSON 报告和 PNG 对照图；不会写入原始文件或既有训练缓存。

## 本轮检查结果

最终运行目录：`synthetic/changan_ecm/20260919_152428_vin102_internal_resistance/`。前两次同参数调试运行也独立保留，未覆盖。

| 检查 | 结果 | 含义 |
|---|---:|---|
| R0/R1/R2 | 0.485/0.358/0.231 mΩ | 仅为该车此段的差分回归估计 |
| 留出电压变化 RMSE | 3.74 mV | 零预测基线为 13.40 mV；只检验正常动态，不验证故障 |
| 电流阶跃 RMSE | 6.37 mV | 留出片段、同一辆车 |
| 故障前电压保持不变 | 通过 | 无故障前泄漏 |
| 电流与故障电压响应方向 | 通过 | 放电时降压、充电时升压 |
| 单体电压 2.5–4.3 V 筛查线 | **未通过** | 一条候选约 4.308 V；此线不是正式 BMS 限值 |
| Pack 单体求和与故障增量守恒 | 通过 | 内部代数检查；不代表实测 Pack 电压完全等于求和 |
| 额外电阻发热积分非负 | 通过 | 只是热功率计算，不是温升校验 |
| 温度探针故障响应 | **未验证** | 没有几何、热容和换热参数，温度轨迹保持实测正常值 |
| 真实故障外部验证 | **未验证** | 没有长安真实内阻异常单体作对照 |
| 源基线健康状态 | **未验证** | 没有逐电芯故障真值 |

因此 `ready_for_training=false`。特别是不能将这 8 条直接追加到 Mamba 分类器训练集。现有结果只说明：在真实工况下，故障电压响应符合所设 ECM 方程，并暴露了高 SOC 充电越界问题；尚不能说明它与真实故障同分布。

## 复现

在 `H:/mamba/code2` 执行：

```powershell
& 'D:\enviroment\anaconda\envs\py312\python.exe' generate_changan_ecm.py
```

每次运行新建时间戳目录，不覆盖上轮。下一阶段应先得到长安电芯的 OCV/EIS 或电流脉冲参数、温度探针几何与热参数，再完成电热耦合；随后用**不同 VIN** 的正常工况和真实故障资料做外部检验。只有质量门槛通过后，才能评估对真实车辆测试集分类性能的增益。
