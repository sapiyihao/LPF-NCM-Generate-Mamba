# 高内阻故障序列生成：首轮试验记录

论文启发：正常/老化数据预训练生成器，然后使用少量故障数据迁移，并以 WGAN-GP 提高对抗训练稳定性。本实现改为 `[512,6]` 卷积式序列 GAN，生成后推导第7通道电压极差；因此不是论文架构的完全复现。

## 数据与隔离

- 只读取 `processed_v2` 的 LFP `train` 划分；源域为280台正常训练车，目标域为21台高内阻训练车。
- 实际加载完整窗口：正常源域约2240个、高内阻目标域1321个。窗口重叠，不等于独立车辆。
- 每轮生成256条候选 `[512,7]` 曲线，仅保存在 `synthetic/` 的独立时间戳目录。原始CSV和缓存没有改动；验证/测试VIN从未参与生成器拟合。
- 生成器保证 `Vmin <= Vmean <= Vmax`、`Tmin <= Tmax`，第7通道严格为 `Vmax - Vmin`。这只是基本数值约束，不代表电化学有效性。

## 两轮结果

| 轮次 | 配置 | 电压/电流分布门槛 | 时间动态门槛 | 可用于分类器 |
|---|---|---|---|---|
| `20260919_144054_LFP_high_resistance` | 正常预训练200步，故障迁移300步，每步2次判别器 | 未通过 | 未通过 | 否 |
| `20260919_144139_LFP_high_resistance` | 正常预训练1000步，故障迁移2000步，每步5次判别器 | 通过 | 未通过 | 否 |

加强训练后，电流分位数偏差从0.542降到0.186（相对于真实训练集尺度），但最低单体电压的平均逐点变化量仅为真实样本的0.242倍；最高/最低温度的变化量约为真实样本的45/119倍。对比图可见不自然的温度抖动和后半段电压振荡。第二轮 69.5% 的候选被既有分类器判为高内阻，但这不能证明真实：分类器也可能对异常波形产生高置信度预测。质量门槛明确拒绝了两批候选，所以没有做“增强后分类器”的测试。

本试验提示：论文所用的定长、对齐、单通道实验室充电曲线，与当前多工况、多通道车辆窗口差异较大；直接生成七通道无条件窗口并不可靠。下一步应优先尝试保持真实电流/温度轨迹、只生成或变换故障相关电压响应，并按完整充电段或工况对齐训练；重新通过质量门槛后，再做真实车辆测试集上的增强前后对照。
